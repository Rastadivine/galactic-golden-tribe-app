// pages/not-aligned.js
import { useRouter } from 'next/router';
import { Button } from '@/components/ui/button';

export default function NotAlignedPage() {
  const router = useRouter();

  return (
    <div className="min-h-screen bg-gradient-to-br from-black via-indigo-900 to-purple-950 flex items-center justify-center text-white px-6">
      <div className="max-w-xl text-center space-y-6 animate-fade-in">
        <h1 className="text-4xl font-bold text-gold-300">Access Not Yet Aligned ✋🏽</h1>
        <p className="text-lg text-gray-300">
          Your wallet has not yet been initiated with a Soulbound Role. Please connect with a Tribe Elder or request an invite.
        </p>
        <Button className="bg-yellow-400 text-black hover:bg-yellow-300" onClick={() => router.push('/connect')}>
          Return to Connect Page
        </Button>
      </div>
    </div>
  );
}
